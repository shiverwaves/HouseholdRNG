# Example environment variables
# Copy to .env and fill in your values

# Database connection (optional - for auto-import in GitHub Actions)
DB_HOST=localhost
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=taxapp_dev

# Data years (optional - override defaults)
PUMS_YEAR=2022
BLS_YEAR=2023
